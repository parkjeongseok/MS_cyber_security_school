#include <stdio.h>

int main(void) {
	// 시작값, 종료값 입력
	int sval, eval, rval;
	
	printf("시작값을 입력하세요. : ");
	scanf("%d", &sval);
	
	printf("종료값을 입력하세요. : ");
	scanf("%d", &eval);
	
	rval = 0;
	int index = sval;
	while((index >= sval)&&(index<=eval)){
		
	}
	
}
